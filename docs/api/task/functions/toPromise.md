[True Myth](../../index.md) / [task](../index.md) / toPromise

# Function: toPromise()

> **toPromise**\<`T`, `E`\>(`task`): `Promise`\<[`Result`](../../result/classes/Result.md)\<`T`, `E`\>\>

Standalone version of [`Task.prototype.toPromise`](../classes/Task.md#topromise).

## Type Parameters

### T

`T`

The type of the value when the `Task` resolves successfully.

### E

`E`

The type of the rejection reason when the `Task` rejects.

## Parameters

### task

[`Task`](../classes/Task.md)\<`T`, `E`\>

## Returns

`Promise`\<[`Result`](../../result/classes/Result.md)\<`T`, `E`\>\>
